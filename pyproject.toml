[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rekwaver"
version = "0.0.0"
description = "RekWaver — RekordBox FLAC -> WAV Converter"
authors = [
  { name = "XeTK" }
]
readme = "README.md"
requires-python = ">=3.10"
